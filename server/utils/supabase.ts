/**
 * File ini tidak lagi diperlukan karena menggunakan @nuxtjs/supabase
 * 
 * Gunakan:
 * - serverSupabaseClient(event) untuk client-side access
 * - serverSupabaseServiceRole(event) untuk service role access
 * - serverSupabaseUser(event) untuk mendapatkan user
 * 
 * Import dari: '#supabase/server'
 */
